package ltd.newbee.mall.controller.mall;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;

/*
@SpringBootTest
public class TestInsertIdController {

	@Autowired    // 自动注入，spring boot会帮我们实例化一个对象
    private JdbcTemplate jdbcTemplate;
 
    @Test
    void contextLoads() {
    }
    
    @Test
    public void insert() {
        //for (int i = 0; i < 10000; i++) {
            String sql = "insert into student(id,name,age,gender,location,nativePlace,TEL,totalScore,className,ranking,blacklist,physical,chemistry,biological)  values (6,'李大嘴',20,'男','上海','北京','19876555555',3,'D',6,'0',1,1,1)";
            jdbcTemplate.execute(sql);        
            System.out.println("成功插入1条数据");
       // }
 
    }

	
}
*/
